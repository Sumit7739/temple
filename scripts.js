// Smooth Scroll to Section
function scrollToSection(id) {
    document.getElementById(id).scrollIntoView({ behavior: 'smooth' });
}

// Dummy Ledger Function
function viewLedger() {
    alert("Redirecting to the expense ledger...");
}
